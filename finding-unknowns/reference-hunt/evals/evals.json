{
  "skill": "finding-unknowns-reference-hunt",
  "version": "1.0.0",
  "cases": [
    {
      "id": "e1",
      "description": "Should trigger: point at existing code",
      "input": "I want a rate limiter like the one in the auth service.",
      "shouldTrigger": true,
      "assertions": ["Reads the reference code", "Produces semantics summary before writing", "Confirms summary with user"]
    },
    {
      "id": "e2",
      "description": "Should trigger: cross-language reference",
      "input": "I need Python implementation of the retry logic from our Go library.",
      "shouldTrigger": true,
      "assertions": ["Extracts semantics, not syntax", "Respects license boundaries"]
    },
    {
      "id": "n1",
      "description": "Not trigger: no reference given",
      "input": "Write a retry decorator in Python.",
      "shouldTrigger": false,
      "assertions": ["Implements standard retry pattern", "Does not search for references"]
    }
  ]
}